import React, { Component } from "react";

class Header extends Component {
    render() {
        return (
            <header>
                React Assignment
            </header>
        );
    }
}

export default Header;